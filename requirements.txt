# TensorFlow is optional: `app.py` uses NumPy + h5py when TensorFlow is absent
# (needed for Streamlit Community Cloud on Python 3.14, where TF wheels do not exist).
streamlit>=1.28.0
numpy>=1.26.0
h5py>=3.11.0
