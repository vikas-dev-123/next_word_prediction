# TensorFlow does not publish wheels for Python 3.14+. On Streamlit Community Cloud you MUST set
# Python to 3.11 or 3.12 (Manage app → Settings → Python version), then redeploy.
streamlit>=1.28.0
tensorflow-cpu>=2.16.1,<2.18
numpy>=1.26.0
